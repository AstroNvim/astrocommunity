# OpenTofu Language Pack

This plugin pack does the following:

- Adds `terraform` Treesitter parser and links it to `opentofu`
- Adds `opentofu` and `opentofu-vars` filetypes
- Adds `tofu validate` linter
